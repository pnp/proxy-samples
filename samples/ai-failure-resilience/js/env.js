/**
 * Environment configuration
 * 
 * WARNING: Never commit real API keys to source control!
 * For production applications, use environment variables or a secure key vault.
 * 
 * To use this sample:
 * 1. Generate a fine-grained personal access token at https://github.com/settings/tokens
 * 2. Grant the token `models:read` permission
 * 3. Replace the placeholder below with your token
 */
export const apiKey = "your-github-token-here";
